---
interface Props {
  n: number;
}
const { n } = Astro.props;
---
<sup class="fn-ref" aria-label={`Footnote ${n}`}>{n}</sup>

<style>
  .fn-ref {
    font-family: var(--font-mono);
    font-variant-numeric: tabular-nums;
    font-size: 0.7em;
    font-weight: 500;
    color: var(--ink);
    background: var(--highlight);
    padding: 0 3px;
    margin-left: 1px;
    /* sup default vertical-align is super; keep that. */
  }
</style>
